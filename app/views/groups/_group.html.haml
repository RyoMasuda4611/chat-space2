- @groups.each do |group|
  %li.group
    = link_to root_path( group_id: group.id ), class: 'group__link' do
      .group__name
        %B
          #{ group.name }
      .group__first-message
        = group.messages.last.body if group.messages.any?
