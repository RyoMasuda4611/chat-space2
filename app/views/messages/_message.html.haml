%li.message
  .message__information
    %span.name.dark-blue.bold
      = message.user.nickname
    %span.date.gray
      = message.created_at.strftime('%Y/%m/%d %H:%M:%S')
  %p.message__body
    = message.body
